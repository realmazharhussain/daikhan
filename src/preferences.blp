using Gtk 4.0;
using Adw 1;

template $PreferencesWindow : Adw.PreferencesWindow {
  Adw.PreferencesPage {
    Adw.PreferencesGroup {
      title: _("Appearance");

      $IdComboRow dark_mode_combo {
        title: _("Color Scheme");
      }
    }
  }
}
