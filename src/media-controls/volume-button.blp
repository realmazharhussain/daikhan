using Gtk 4.0;
using Adw 1;

template $VolumeButton : Adw.Bin {
  child: Gtk.VolumeButton {
    adjustment: Adjustment adjustment {
      lower: 0;
      upper: 1;
      step-increment: 0.01;
      page-increment: 0.05;
    };
  };
}
