using Gtk 4.0;
using Adw 1;

template MediaControls : Adw.Bin {
  child: Box {
    orientation: horizontal;
    styles ["toolbar"]

    .PlayButton play_btn {}
    .ProgressBar progress_bar {}
    .VolumeButton volume_btn {}
  };
}
